## Resend (Waitlist)
# Create an API key in Resend dashboard and set it in Vercel (Project → Settings → Environment Variables)
RESEND_API_KEY=

# Sender address shown in "From".
# In production, use an address on a verified domain in Resend (recommended).
# Example: VcodingList <hello@vcodinglist.com>
WAITLIST_FROM_EMAIL=

# Optional: internal notification email (you/admin). If set, every signup also notifies this address.
# Example: contact@vcodinglist.com
WAITLIST_NOTIFY_EMAIL=

